
var lib = require( 'three-to-screen-xy' );
var expect = require( 'expect.js' );

describe( 'three-to-screen-xy', function() {

  it( 'should return Hello World', function() {
    expect( lib() ).to.be( 'Hello World' );
  } );

} );
